name: CI Monorepo Check

on:
  pull_request:
    paths:
      - "cli/**"
      - "devdocs/**"
      - "website/**"
      - "justfile"
      - "cli/justfile"
      - "devdocs/justfile"
      - "website/justfile"
      - ".github/workflows/ci-monorepo-check.yml"
  push:
    branches:
      - main
    paths:
      - "cli/**"
      - "devdocs/**"
      - "website/**"
      - "justfile"
      - "cli/justfile"
      - "devdocs/justfile"
      - "website/justfile"
      - ".github/workflows/ci-monorepo-check.yml"
  workflow_dispatch:

jobs:
  check:
    name: Root just check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Just
        uses: taiki-e/install-action@just

      - name: Setup Dart
        uses: dart-lang/setup-dart@v1

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: devdocs/package-lock.json

      - name: Install CLI dependencies
        run: just cli::deps

      - name: Install DevDocs dependencies
        run: npm ci --prefix devdocs

      - name: Check CLI formatting
        run: dart format --output=none --set-exit-if-changed cli

      - name: Run monorepo quality gate
        run: just check

      - name: Verify local docs output (act)
        if: ${{ env.ACT }}
        run: ls -la devdocs/.vitepress/dist

      - name: Upload docs artifact
        if: ${{ !env.ACT }}
        uses: actions/upload-artifact@v7
        with:
          name: devdocs-site
          path: devdocs/.vitepress/dist
          if-no-files-found: error
