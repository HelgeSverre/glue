# ── OS ──────────────────────────────────────────────────────────────────────
.DS_Store

# ── Dart / pub workspace ────────────────────────────────────────────────────
.dart_tool/
pubspec.lock

# ── Build outputs ──────────────────────────────────────────────────────────
# AOT binary lands at <repo>/dist/glue (via `just cli::build`).
dist/
build/
cli/doc/api/

# Generated source files (rebuilt by `just gen` in each package).
packages/glue_harness/lib/src/config/build_info_generated.dart

# ── IDE / editors ──────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── Environment / secrets ──────────────────────────────────────────────────
.env
.env.local
settings.local.json

# ── Worktrees ──────────────────────────────────────────────────────────────
.worktrees/

# ── Browser / scraping caches ──────────────────────────────────────────────
.local-chrome/
.playwright-mcp/
playwright-report/
test-results/
browser/probe/browser/

# ── JS / website ───────────────────────────────────────────────────────────
node_modules/
.vercel/

# ── Misc tools ─────────────────────────────────────────────────────────────
.junie*
.aider*
.agents/
.claude/
skills-lock.json
mlflow.db
