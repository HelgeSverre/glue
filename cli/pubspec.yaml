name: glue
description: The coding agent that holds it all together.
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  args: ^2.4.0
  http: ^1.2.0
  path: ^1.9.0
  yaml: ^3.1.0
  crypto: ^3.0.0
  cli_completion: ^0.5.1

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
