name: glue
description: The coding agent that holds it all together.
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  args: ^2.7.0
  html: ^0.15.5
  http: ^1.6.0
  path: ^1.9.1
  yaml: ^3.1.3
  crypto: ^3.0.7

dev_dependencies:
  test: ^1.30.0
  lints: ^4.0.0
