# Handoff — branch `refactor/c1-turn` → next work

**Context for a fresh agent session picking this up.**

## Where we are

- Branch `refactor/c1-turn`, 27 commits ahead of `origin/main`.
- Structural refactor complete: Groups A + B + C (C1–C5) + scoped D1.
- `Agent` (was `AgentCore`) now holds `runHeadless`; `Subagents` (was `AgentManager`) spawns them. `AgentRunner` class is gone.
- `lib/glue.dart` barrel deleted; `bin/` + tests import from `package:glue/src/...` directly.
- Part files in `app/`: only `controllers.dart` (152 LoC) + `paint.dart` (210 LoC) remain.
- Analyzer at zero issues, 1511 tests passing, binary builds.
- Full detail in `REFACTOR.md` at the repo root.

## What's unmerged on origin/main but not yet in this branch

Main moved forward while the refactor was in progress. One significant addition:

- **`e8360e2 feat: add context window management system`** — 1500-line addition by a Copilot SWE agent: new `cli/lib/src/context/` module (ContextManager, ContextBudget, ContextEstimator, ToolResultTrimmer, ConversationCompactor, SlidingWindowTrimmer, OverflowClassifier), plus `/compact` slash command, plus `GlueConfig.contextConfig`, plus 45 unit tests in `test/context/`. Wires into `AgentCore.run` to prep context before each LLM call and retry on overflow.

This was built against the pre-refactor shape: `AgentCore` (renamed to `Agent`), `glue.dart` barrel (deleted), `service_locator.dart` adapter registry (unchanged). Direct merge will surface conflicts and broken imports. See Task 5.

## Repo pointers

- `REFACTOR.md` (repo root) — living plan; What's Done section is current.
- `docs/plans/2026-04-24-cli-roadmap.md` — next 3 features (git commands, image paste, MCP).
- `docs/plans/` more generally — has plans for prompt caching, observability, auth flows, text selection, ACP webui, etc. Some landed, some pending. Cross-check against git log.
- `cli/lib/src/runtime/` — the refactor's new shape: `turn.dart`, `renderer.dart`, `transcript.dart`, `input_router.dart`, `permission_gate.dart`, `services/{config,session}.dart`, `controllers/{chat,model,provider,session,skills,system}_controller.dart`.
- `cli/lib/src/agent/agent.dart` — `Agent` class + all domain types (`LlmClient`, `Message`, `AgentEvent`, `Tool`, `ToolApprovalPolicy`, `LlmChunk` hierarchy).
- `cli/lib/src/providers/` — 4 adapters (Anthropic, OpenAI-compatible, Ollama, Copilot) + `LlmClientFactory`.
- `cli/lib/src/llm/` — 3 concrete clients (Anthropic, OpenAI, Ollama) + `MessageMapper`.
- `cli/lib/src/commands/arg_completers.dart` — the arg-completer pattern (module-level functions returning `List<SlashArgCandidate>`).
- `cli/lib/src/runtime/commands/register_builtin_slash_commands.dart` — where every `/command` is declared.

## Task list below

Each numbered task is independent. Rough order by risk/value: 1 → 2 → 4 → 3 → 5 → 6. Task 5 (context window) should probably be done after main is merged into this branch and conflicts resolved.
