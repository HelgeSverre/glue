name: glue_server
description: Surface adapters that expose the harness over external protocols (ACP, MCP, …).
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  glue_core:
    path: ../glue_core

dev_dependencies:
  test: ^1.30.0
  lints: ^6.1.0
